let runTests;
export function registerTestRunner(testRunner) {
    runTests = testRunner;
}
export async function executeWebpackTests() {
    await (runTests === null || runTests === void 0 ? void 0 : runTests());
}
