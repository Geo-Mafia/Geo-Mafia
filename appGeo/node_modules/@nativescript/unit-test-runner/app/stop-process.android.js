import { Application } from '@nativescript/core';
export function killProcess() {
    var _a, _b;
    if (android.os.Build.VERSION.SDK_INT >= 21) {
        (_a = Application.android.startActivity) === null || _a === void 0 ? void 0 : _a.finishAndRemoveTask();
    }
    else {
        (_b = Application.android.startActivity) === null || _b === void 0 ? void 0 : _b.finishAffinity();
    }
    java.lang.System.exit(0);
}
